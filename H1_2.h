#pragma once
#include <iostream>

using namespace std;

template<class T>
	T sum(T a[], int start, int end, T afw = 0);

template<class TT>
	TT lsum(TT a[], int start, int end, TT afw = 0);
